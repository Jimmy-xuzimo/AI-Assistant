# AI 助手前端性能与稳定性优化计划

## 目标与现状

你提到的核心问题有 3 个：
1. 页面速度慢、动画不流畅。
2. 历史记录体验有问题。
3. 对话记录偶尔消失。

结合当前实现，主要风险点是：
- 大量动画、模糊与全局过渡导致主线程和渲染压力较高。
- 会话只在助手回复后才持久化，用户消息在异常场景可能丢失。
- 刷新后默认“新会话”而不是恢复最近会话，容易被理解为“记录消失”。

---

## 第一阶段（P0，1~2 天）：先止损，先把“丢记录”和卡顿明显改善

### 1) 会话可靠性修复（最高优先级）

- **发送后立即保存用户消息**：用户点击发送后（push user message）立刻保存，而不是等待 AI 回复成功。
- **失败回复也入库**：API 错误时也要把错误状态写入会话（例如 `status: failed`），保证时间线完整。
- **草稿自动保存**：输入框内容按节流（如 300ms）写入 `localStorage`，误刷新不丢输入。
- **加载策略改为“恢复最近会话”**：页面初始化优先加载最近一条会话，而不是直接新建空会话。
- **加入数据版本号**：`chats_v2`，启动时做一次迁移，避免后续结构变更导致读取失败。

### 2) 动画与渲染减负（立即可感知）

- **移除全局 `*` 过渡**：改成只给交互关键元素做过渡，避免每个节点都参与动画计算。
- **降低模糊开销**：减少 `backdrop-filter` 和大半径 `blur`（尤其移动端）。
- **控制无限动画数量**：背景漂浮、消息脉冲等减少到 1~2 个核心动效。
- **支持无障碍/低性能模式**：加 `prefers-reduced-motion`，在低端设备自动降级动画。

### 3) 历史记录交互修复

- **历史项虚拟化或分页**：超过一定数量（如 50）时按页加载，避免一次性渲染全部。
- **删除流程优化**：删除后保持选中状态一致（如果删当前会话则自动切到下一条）。
- **稳定排序**：按 `updatedAt` 排序，而不是只用创建时间。

---

## 第二阶段（P1，2~4 天）：性能工程化与可观测

### 4) 建立性能基线（先量化再优化）

- 指标：
  - 首屏可交互时间（TTI）
  - 输入后首字符响应延迟
  - 单次消息发送到渲染完成耗时
  - 长会话滚动 FPS
- 工具：
  - Chrome Performance 录制（桌面 + 手机模拟）
  - Lighthouse（Performance + Best Practices）

### 5) 渲染路径优化

- **Markdown 渲染延后**：对长回答采用分段渲染（chunk append）。
- **滚动优化**：消息追加后仅在“用户处于底部附近”时自动滚到底。
- **事件与 DOM 更新节流**：避免高频重排（特别是输入、历史列表更新）。

### 6) 存储架构升级

- `localStorage` -> `IndexedDB`（会话多时更稳定，避免同步阻塞）。
- 会话结构示例：
  - `conversations`（元信息）
  - `messages`（按 conversationId 索引）
- 增加“写入队列 + 重试”，防止偶发写入失败。

---

## 第三阶段（P2，1 周）：产品成熟度提升

### 7) 可靠性与恢复能力

- **未完成消息恢复**：刷新后识别 in-flight 消息并提示“是否重试发送”。
- **导出/导入会话**：JSON 备份，降低用户对“记录丢失”的焦虑。
- **多标签页一致性**：监听 `storage` 事件，保持不同 tab 的历史同步。

### 8) 体验升级（成熟 AI 助手应具备）

- 对话搜索（标题+内容全文检索）
- 会话置顶、重命名、归档
- Token/字数统计、回答耗时提示
- 网络状态提示（离线/重连）

### 9) 安全与部署建议

- 前端直连第三方 API key 风险高，建议改为你服务器做中转（隐藏密钥、限流、审计）。
- 增加错误日志上报（Sentry 或自建）定位“偶发消失”场景。

---

## 建议的里程碑与验收标准

### M1（本周）
- 完成 P0。
- 验收：
  - 刷新后会话可恢复。
  - API 失败不丢用户消息。
  - 中端手机交互明显更流畅。

### M2（下周）
- 完成 P1。
- 验收：
  - Lighthouse Performance 提升到 80+（移动端）。
  - 1000 条消息历史仍可流畅打开与滚动。

### M3（第 3 周）
- 完成 P2 关键项。
- 验收：
  - 具备导入导出、搜索、恢复重试能力。
  - 线上错误可追踪，故障可复盘。

---

## 你这个项目中最值得优先改的 5 件事（按收益排序）

1. **发送后立刻持久化用户消息**（解决“记录消失”体感最直接）。
2. **启动时恢复最近会话而不是新建会话**。
3. **砍掉全局过渡与重模糊效果**（解决“动画不流畅”）。
4. **历史记录分页/虚拟化**（解决“历史区域问题”）。
5. **将 API key 移到后端代理**（长期稳定性与安全性）。

如果你愿意，我下一步可以按这个计划直接给你出一个 **P0 可落地改造清单（到函数级别）**，你可以边改边验收。
